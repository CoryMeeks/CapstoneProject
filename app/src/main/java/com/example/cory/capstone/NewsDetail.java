package com.example.cory.capstone;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

public class NewsDetail extends AppCompatActivity {

    /*
     * TODO:
     *      Create a way to get users to write posts
     *      Create a way to let users upvote or downvote other posts
     *      Create a way to let users track posts, and follow users
     *      Create a way to organize posts, either by date, relevance to topic, etc
     */

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_news_detail);
    }
}
